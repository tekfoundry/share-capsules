# Production Change Ledger

This ledger tracks the sanitized operational delta between the repository and the existing Share Capsules production environment. It is maintained as implementation progresses even when deployment is intentionally deferred.

Do not record passwords, tokens, private keys, connection strings, secret values, customer data, or other credentials here. Record environment-variable names and the required action only. Secret values belong in the production secret-management system.

## Deployment policy

- A completed development phase does not automatically trigger a production deployment.
- Deploy only after an explicit review confirms that the release contains a coherent, user-visible capability worth operating.
- Before deployment, reconcile this ledger with `code/.env.production.example`, migrations, scheduled commands, queue requirements, external identities, and infrastructure changes.
- Every deployed change requires verification and a rollback or forward-recovery decision.

## Existing production baseline

The production environment predates this ledger. Its sanitized configuration and operational baseline still need to be reconciled against the repository before the next production deployment.

## Pending changes

Add an entry when implementation introduces a future production requirement, even if no deployment is planned yet.

| Date       | Change                                                             | Production action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Rollback or recovery                                                                                                                                                                                                                                                                                                                | Status  |
| ---------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| 2026-06-21 | Establish production change tracking                               | Inventory the existing environment against `code/.env.production.example`; record variable names and infrastructure dependencies without values                                                                                                                                                                                                                                                                                                                                                                                                      | Production configuration passes deployment validation and `/up` reports healthy after the next approved deployment                                                                                                                                                                                                                                                                                                                                                                                                  | Preserve the current environment until reconciliation is complete                                                                                                                                                                                                                                                                   | Pending |
| 2026-06-21 | Add CTX provider discovery and signing-key lifecycle               | Apply the `ctx_ticket_signing_keys` migration; ensure the every-minute Laravel scheduler is running; stage a ticket-signing key, verify JWKS publication, then activate it using the rotation runbook                                                                                                                                                                                                                                                                                                                                                | Discovery returns the exact configured HTTPS issuer and CTX V1 capabilities; JWKS exposes only the staged public key; the active key remains decryptable by the application                                                                                                                                                                                                                                                                                                                                         | Roll back application code before the migration when unused; never remove a key that may have signed a still-valid ticket; preserve application-key decryption access during recovery                                                                                                                                               | Pending |
| 2026-06-21 | Add the separately deployable Key Broker boundary                  | Create a broker runtime from `code/.env.broker.production.example`; set a broker-specific `APP_KEY`; add `SHARECAPSULES_COMPONENT`, the public `SHARECAPSULES_BROKER_URL`, the control-plane-only `SHARECAPSULES_BROKER_INTERNAL_URL`, `SHARECAPSULES_BROKER_CONTROL_PLANE_TOKEN`, `SHARECAPSULES_BROKER_AUDIT_CHANNEL`, `BROKER_AUDIT_LOG_LEVEL`, and the `BROKER_DB_*` variables; provision a dedicated database/user; route broker audit output separately; give the control plane only the shared service credential, not broker database access | Broker `/up` is healthy; the route table contains only the intended broker routes; discovery matches the HTTPS broker identity; the control plane reaches the lifecycle API through the internal URL; internal access rejects missing/incorrect credentials and accepts the configured credential; sanitized rejection events reach the restricted audit destination                                                                                                                                                | Keep the existing production environment unchanged until the value gate; remove the broker runtime and its service credential if rolled back; retain or securely destroy the empty broker database according to deployment state                                                                                                    | Pending |
| 2026-06-21 | Add broker key-custody adapter                                     | Select a managed KMS/HSM product before production; set `BROKER_KMS_DRIVER=managed` and `BROKER_KMS_KEY_ID` only in the broker environment; grant the broker runtime encrypt/decrypt access to that purpose-bound key while withholding access from the control plane                                                                                                                                                                                                                                                                                | Broker production health rejects the local driver; managed custody operations pass the published key-protection contract and audit checks before content-key registration is enabled                                                                                                                                                                                                                                                                                                                                | Disable registration and release if managed custody is unavailable; preserve encrypted records and key-version access during rollback or key rotation                                                                                                                                                                               | Pending |
| 2026-06-21 | Add content-key registration                                       | Apply the control-plane `broker_registration_grants` migration and isolated broker migration path; add `SHARECAPSULES_BROKER_CALLBACK_TOKEN` to both runtimes and `SHARECAPSULES_CONTROL_PLANE_INTERNAL_URL` to the broker; permit broker-to-control-plane HTTPS access while withholding broker database and custody credentials from the control plane                                                                                                                                                                                             | A DPoP-bound `capsule:create` token issues a 60-second digest-bound grant; a valid registration returns a 43-character opaque handle; a fabricated grant fails closed; broker storage contains protected material and no plaintext content key; control-plane isolation checks pass                                                                                                                                                                                                                                 | Disable the public registration endpoint before rollback; preserve broker ciphertext and managed-key access for already-issued handles; remove unused unredeemed grants after the previous release is restored                                                                                                                      | Pending |
| 2026-06-21 | Add CTX policy evaluation and ticket issuance                      | Apply `ctx_authorization_tickets` and CTX release-counter migrations plus the isolated broker ticket-binding migration; ensure an active ticket-signing key is staged and activated; permit the control plane to reach the broker internal binding endpoint using the existing control-plane credential                                                                                                                                                                                                                                              | A DPoP-bound `ctx:authorize` request reproduces the published policy vector, verifies the registered broker binding, returns a verifiable 60-second Ed25519 ticket with no account subject, and stores one pending private mapping; missing signing keys and mismatched bindings fail closed                                                                                                                                                                                                                        | Disable `/ctx/authorize`; preserve signing-key decryption access and pending records through their short expiry; do not remove a public key while an issued ticket may remain valid                                                                                                                                                 | Pending |
| 2026-06-21 | Add broker key release and atomic redemption                       | Apply the broker device-proof replay migration; configure `SHARECAPSULES_CTX_ISSUER` and broker-only `SHARECAPSULES_CTX_INTERNAL_URL`; permit broker access to provider JWKS and the authenticated redemption endpoint; schedule expiry pruning before deployment                                                                                                                                                                                                                                                                                    | Automated protocol and isolated runtime checks pass; replay and expired tickets do not count; concurrent tickets cannot exceed either limit; no plaintext key enters storage or logs. Complete the separate Viewer integration test after the extension exists and before a user-facing release                                                                                                                                                                                                                     | Disable `/releases` before rollback; retain protected keys and counter state; preserve ticket signing keys until outstanding tickets expire; investigate any ambiguous committed release rather than decrementing authoritative counters                                                                                            | Pending |
| 2026-06-21 | Add provider-aware CTX metrics foundation                          | Apply `2026_06_21_092000_create_ctx_metric_foundation.php`; keep the existing daily Laravel scheduler running so raw metric events older than 30 days are pruned; no new environment variables or additional process are required                                                                                                                                                                                                                                                                                                                    | Repeated delivery of one event changes projections once; provider partitions remain separate; committed-release totals match authoritative redemption behavior; metric tables contain no account, Viewer-device, proof, ticket, or raw evidence columns                                                                                                                                                                                                                                                             | Disable metric recording and creator metric reads before rollback; preserve authoritative release counters; retain projections for investigation or archive them according to the approved retention policy rather than treating them as release authority                                                                          | Pending |
| 2026-06-21 | Add Capsule and account key lifecycle                              | Apply broker migration `0001_01_01_000003_add_lifecycle_to_broker_content_keys.php`; allow the existing control plane to call the authenticated broker lifecycle endpoint using the existing control-plane credential; no new environment variables or processes are required                                                                                                                                                                                                                                                                        | Closing an account blocks releases and pauses active broker records; restoration resumes only paused records; Capsule revocation cannot be reversed; permanent deletion and deletion-ledger replay null protected key material, custody metadata, and creator linkage; broker failure prevents local permanent deletion and restore readiness from completing                                                                                                                                                       | Disable authorization and `/releases` before rollback; do not restore destroyed key material; preserve the deletion ledger and retry incomplete destruction; a database rollback is unsafe after any record has been destroyed because the removed content key cannot be reconstructed                                              | Pending |
| 2026-06-21 | Add deterministic V1 automation-risk enforcement                   | Apply `2026_06_21_093000_create_ctx_automation_risk_foundation.php`; keep the existing daily scheduler running for 30-day activity and assessment pruning; no new environment variables or processes are required; complete threshold calibration, false-positive, privacy, and viewer-explanation review before enabling automation-risk-gated Capsules in production                                                                                                                                                                               | Exact boundary tests pass for every `ctx-automation-risk-v1.0` rule; unaccepted issuers fail closed; authorization and redemption both recompute current risk; retained columns match the reviewed CTX-only schema; pruning and account-deletion cascade remove identifiable risk state                                                                                                                                                                                                                             | Stop issuing policies with the automation-risk predicate before rollback; preserve ordinary authorization and release behavior for policies without the gate; archive only sanitized aggregate calibration results and remove account-linked rolling state according to the 30-day retention rule                                   | Pending |
| 2026-06-21 | Add privacy-safe Viewer denial presentation                        | Deploy the matching control-plane, broker, and future Viewer-extension contract together; no migration, environment variable, or additional process is required; keep Host integrations restricted to the generic lifecycle projection                                                                                                                                                                                                                                                                                                               | All 17 V1 codes parse and map exhaustively; broker redemption preserves only reviewed authenticated denials and rejects malformed internal responses; device-proof and ticket failures remain distinct; Viewer copy exposes no identity, score, threshold, or history; Host output contains only `locked`, `unavailable`, or `unsupported`; creator metrics retain only coarse categories                                                                                                                           | Roll back control plane, broker, and Viewer mapping as one compatible unit; if versions temporarily differ, fail unknown responses to generic temporary unavailability and never forward a raw internal body to the Host                                                                                                            | Pending |
| 2026-06-21 | Add creator-selected Capsule access windows                        | Apply `2026_06_21_094000_add_access_window_to_ctx_authorization_tickets.php`; deploy the matching policy contract, control plane, and future creator/viewer extension together; no new environment variable or process is required; monitor production clock synchronization as a security dependency                                                                                                                                                                                                                                                | Canonical UTC instants parse identically in TypeScript and PHP; authorization allows the inclusive opening instant and rejects the exclusive closing instant; redemption repeats both checks; blank Creator Studio dates omit the gate                                                                                                                                                                                                                                                                              | Stop creator tooling from issuing the access-window predicate before rollback; preserve existing ticket rows until their short expiry; policies already signed with the predicate require a compatible provider and Viewer and must fail closed under older code                                                                    | Pending |
| 2026-06-22 | Add authoritative Capsule registry and pending broker finalization | Apply control-plane migration `2026_06_22_010000_create_creator_capsules_table.php` and broker migration `0001_01_01_000004_add_pending_finalization_to_broker_content_keys.php`; set `CAPSULE_PENDING_TTL_MINUTES` in both runtimes; deploy control plane, broker, and Creator extension as one compatible contract; keep the existing scheduler enabled for `capsules:clean-pending` every five minutes                                                                                                                                            | A registration creates one creator-owned immutable pending revision and non-releasable broker record; strict local archive verification plus idempotent finalization converges both to active; cancellation, expiry, signing/packaging failure, lost responses, and retries destroy or recover pending material without orphaned releasable keys; inventory, policy summaries, revocation, metrics limits, closure, deletion, and restore replay use the registry rather than grants, tickets, counters, or metrics | Disable new registration and finalization before rollback; allow outstanding pending records to finalize under the compatible release or destroy them; never downgrade active/revoked/destroyed lifecycle facts into inferred grant state; preserve deletion obligations and broker custody access until cleanup is proven complete | Pending |
| 2026-06-22 | Add human-readable Capsule inventory metadata                      | Apply `2026_06_22_020000_add_inventory_metadata_to_creator_capsules.php`; deploy the matching Creator extension registration contract and Laravel inventory together; no new environment variables or processes are required                                                                                                                                                                                                                                                                                                                         | New registrations retain the public title, content-profile identity/version, and media type without source bytes; legacy rows render safe fallbacks; creators can set an account-only management label; ownership tests prevent cross-account edits                                                                                                                                                                                                                                                                 | Roll back display and registration code before dropping nullable metadata columns; preserve Capsule lifecycle and broker records; management labels may be exported before rollback if users have begun relying on them                                                                                                             | Pending |
| 2026-06-22 | Gate per-account limit-pressure metrics behind privacy approval    | Ship creator pressure output disabled by default; if later approved, record the versioned cohort, bucket, pressure, and retention configuration names and deploy matching tests and disclosure text; do not backfill deferred audience dimensions                                                                                                                                                                                                                                                                                                    | With the feature disabled, no per-account pressure value is rendered; approved configuration suppresses every below-threshold cohort and exposes only aggregate counts; event and projection storage continue to contain empty optional dimensions and no viewer identifier                                                                                                                                                                                                                                         | Disable the creator-facing pressure projection without changing authoritative enforcement counters; remove any unapproved aggregate material under the documented retention rule                                                                                                                                                    | Pending |
| 2026-06-22 | Add the development Manifest V3 Creator runtime                    | Keep the committed development extension public key and ID out of production; build the unpacked extension with `npm run build:extension`; provision the development OAuth callback only in the local environment; reserve the production extension ID, public key, OAuth callback, and Web Store signing/release process for a separately reviewed production release                                                                                                                                                                               | The unpacked build reports ID `dhconceamghcnndjodjhjikknblhkmej`; its manifest contains only packaged code, reviewed permissions, local automatic origins, and optional HTTPS grants; OAuth registration uses the exact matching development callback; the Creator recovery acceptance test passes                                                                                                                                                                                                                  | Remove or disable the development OAuth registration without changing production identity; clear development extension storage and rebuild from source; never reuse the development manifest key or callback as the production publishing identity                                                                                  | Pending |
| 2026-06-22 | Add creator-requested per-Capsule deletion                        | Deploy the matching control-plane and broker lifecycle contract together; no migration, environment variable, or additional process is required; retain the existing five-minute `capsules:clean-pending` schedule for ambiguous destruction retries                                                                                                                                                                                                                                                                 | Deleting an owned active or revoked revision immediately prevents release, destroys and unlinks only its broker-held content key, hides it from inventory, and retains a terminal control-plane tombstone; cross-account requests fail; downloaded and hosted encrypted files remain outside the deletion boundary                                                                                                                                                | Disable the creator delete route before rolling back either runtime; allow `cleanup_pending` records to finish under a compatible broker release; never restore destroyed key material or convert a destroyed tombstone back to active                                                                                              | Pending |
| 2026-06-25 | Add Trust Capsule challenge scoring and step-up flow               | Apply `2026_06_25_120000_add_usage_scoring_to_ctx_automation_risk_assessments.php`, `2026_06_25_130000_create_ctx_challenge_attempts.php`, `2026_06_25_140000_add_challenge_binding_to_ctx_authorization_tickets.php`, and `2026_06_25_150000_add_retention_metadata_to_ctx_challenge_attempts.php`; keep the scheduler running so challenge attempts prune after the reviewed retention window; treat challenge thresholds, score expiry, active registry modules, and automation-risk limits as code-versioned operational knobs documented in `trust-capsule-challenge-operations.md` | Low-history eligible viewers receive `challenge_required`; a completed provider-hosted challenge allows the original Trust Capsule only while evidence is current and bound; active severe usage risk still blocks authorization and redemption; challenge storage contains only reviewed metadata and prunes after expiry plus 24 hours; static-host and playground challenge fixtures pass | Stop issuing new Trust Capsule policies or disable challenge attempt creation before rollback; preserve pending tickets through their short expiry; retain challenge attempts only for the reviewed retention window; if a module is unsafe, remove it from the active registry and fail closed until a reviewed replacement pool is available | Pending |
| 2026-06-26 | Add broker-origin routing for prototype deployment                 | Configure a distinct broker DNS name and `SHARECAPSULES_BROKER_URL`; allow the broker origin to resolve either to the same Laravel installation as the control plane for prototype validation or to a separate broker-only installation for hardened deployment; keep broker internal routes protected by the service credential and retain broker audit configuration                                                                                                                                                                                        | Requests to the app host expose control-plane routes and health; requests to the broker host expose only the broker route surface and broker health; broker metadata, ticket audience, DPoP target, registration, release, lifecycle, and audit behavior use the configured broker origin exactly; same-install hosting is documented as reduced isolation                                                                                                                    | Move the broker DNS name to a separate broker-only installation for isolation without changing Capsule protocol identity; if broker-origin routing fails, disable key registration and release while keeping the app host online                                                                                                    | Pending |

## Deployed changes

Move entries here only after the production action and verification succeed. Include the release or commit identifier and deployment date.

| Deployed at | Release | Change | Verification | Recovery notes |
| ----------- | ------- | ------ | ------------ | -------------- |
